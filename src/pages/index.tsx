import { FC } from 'react';
import { LangButtonts } from '@/components/ui/LanguageButtons';

const Home: FC = () => {
  return (
    <div>
      <LangButtonts />
    </div>
  );
};

export default Home;
